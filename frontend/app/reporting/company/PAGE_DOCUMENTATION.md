# Документация страницы: Отчет по компании (reporting/company/page.tsx)

## Общее описание

Страница детального отчета по компании. Отображает подробную аналитику по рекрутингу, метрики эффективности и статистику.

## Компоненты страницы

### 1. Заголовок отчета
- **Компонент**: `Text` с названием
- **Назначение**: Заголовок страницы отчета
- **Логика**: Отображает "Отчет по компании"

### 2. Разделы отчета
- **Компоненты**: Различные секции с метриками
- **Назначение**: Отображение детальной аналитики
- **Разделы**:
  - Общие метрики
  - Метрики по вакансиям
  - Метрики по заявкам
  - Метрики по интервьюерам
  - SLA метрики
  - Метрики скорости
  - Метрики результатов
- **TODO**: Реализовать все разделы отчета

### 3. Графики и диаграммы
- **Компоненты**: Компоненты визуализации данных
- **Назначение**: Визуальное отображение метрик
- **Типы**: 
  - Линейные графики
  - Столбчатые диаграммы
  - Круговые диаграммы
- **TODO**: Реализовать графики

## Интерфейсы и типы

### ReportMetrics
```typescript
interface ReportMetrics {
  totalScreenings: number
  totalInterviews: number
  conversion: number
  totalTime: string
}
```

### TableDataItem
```typescript
interface TableDataItem {
  period: string
  screenings: number
  interviews: number
  total: number
  time: string
}
```

## Состояние компонента

### State переменные:
- `period`: тип периода отчета ('daily' | 'weekly' | 'monthly' | 'quarterly' | 'yearly')
- `startDate`: дата начала периода (DD.MM.YYYY)
- `endDate`: дата окончания периода (DD.MM.YYYY)
- `graphPeriod`: период графика ('months' | 'weeks' | 'days')
- `graphUnit`: единица измерения ('hours' | 'count')
- `graphFilter`: фильтр графика ('vacancies' | 'recruiters')
- `selectedVacancies`: массив ID выбранных вакансий для графика

## Функции и обработчики

### handleExportExcel()
- Экспорт отчета в Excel
- TODO: Реализовать экспорт через API или библиотеку

### handleVacancyToggle(vacancyId)
- Переключение выбора вакансии на графике
- Обновляет selectedVacancies

## Вычисляемые значения

### metrics
- Моковые данные метрик (totalScreenings, totalInterviews, conversion, totalTime)
- TODO: Вычислять из реальных данных

### tableData
- Моковые данные таблицы по периодам
- TODO: Загружать из API

## Моковые данные

### mockMetrics
Объект с метриками для отчета:
- Общие метрики
- Метрики по вакансиям
- Метрики по заявкам
- И другие разделы

**TODO**: Заменить на реальные данные из API

## TODO: Замена моковых данных на API

1. ❌ `mockMetrics` - заменить на загрузку из API
2. ❌ `loadMetrics()` - реализовать реальный API вызов
3. ❌ Фильтры отчета - реализовать функциональность
4. ❌ Графики - реализовать визуализацию данных
5. ❌ Экспорт отчета - реализовать экспорт в Excel/PDF

## Стили

- **Файл**: `company-report.module.css`
- **Основной контейнер**: Стили для layout страницы отчета
- **Графики**: Стили для визуализации данных

## Связи с другими страницами

- `/reporting/page.tsx` - главная страница отчетности (кнопка "Назад")
- `/reporting/hiring-plan` - план найма

## Особенности

- Детальная аналитика по компании
- Визуализация данных через графики
- Фильтрация по периоду и другим параметрам
- Экспорт отчета (TODO: реализовать)
