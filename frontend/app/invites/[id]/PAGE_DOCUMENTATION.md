# Документация страницы: Детальный просмотр инвайта (invites/[id]/page.tsx)

## Общее описание

Страница детального просмотра инвайта на интервью. Отображает полную информацию об инвайте, позволяет управлять им (копирование текста, пересоздание scorecard, редактирование, удаление).

## Компоненты страницы

### 1. Заголовок страницы
- **Компонент**: `Flex` с кнопкой "Назад"
- **Назначение**: Навигация и заголовок страницы
- **Логика**: 
  - Кнопка "Назад" возвращает на страницу списка инвайтов
  - Отображает название страницы "Детали инвайта"
- **Связи**: Навигация на `/invites`

### 2. Карточка информации об инвайте
- **Компонент**: `Card` с информацией
- **Назначение**: Отображение основной информации об инвайте
- **Секции**:
  - Статус инвайта (Badge с цветовой индикацией)
  - Информация о кандидате (имя, email, ссылка в Huntflow)
  - Информация о вакансии (название)
  - Время интервью (дата и время)
  - Формат интервью (Онлайн/Офис)
  - Ссылки (Google Meet, Google Calendar, Scorecard)
- **Логика**: Отображает все данные инвайта в структурированном виде

### 3. Кнопки действий
- **Компонент**: `Button` компоненты
- **Назначение**: Управление инвайтом
- **Действия**:
  - Копировать текст инвайта
  - Пересоздать Scorecard
  - Редактировать инвайт
  - Удалить инвайт
- **Логика**: 
  - Каждая кнопка вызывает соответствующую функцию
  - Показывает toast уведомления об успехе/ошибке

## Состояние компонента

### State переменные:
- `invite`: данные инвайта для отображения (тип: `Invite | null`)
- `loading`: флаг загрузки данных

## Функции и обработчики

### loadInvite()
- **Назначение**: Загрузка данных инвайта по ID
- **Логика**: 
  1. Устанавливает `loading = true`
  2. Вызывает `invitesApi.getById(id)`
  3. Обновляет состояние
  4. При ошибке показывает toast и редиректит на список
- **Вызывается**: 
  - При монтировании компонента (useEffect)
  - При изменении ID из URL
- **TODO**: Реализовать реальный API вызов

### handleCopyInvitation()
- **Назначение**: Копирование текста инвайта в буфер обмена
- **Логика**: 
  1. Получает текст инвайта через `invitesApi.getInvitationText(id)`
  2. Копирует в буфер обмена через `navigator.clipboard.writeText()`
  3. Показывает toast об успехе
- **Связи**: API `invitesApi.getInvitationText()`

### handleRecreateScorecard()
- **Назначение**: Пересоздание Scorecard в Google Drive
- **Логика**: 
  1. Отправляет запрос на пересоздание через API
  2. Показывает toast об успехе/ошибке
  3. Обновляет данные инвайта после пересоздания
- **TODO**: Реализовать реальный API вызов

### handleDelete()
- **Назначение**: Удаление инвайта
- **Логика**: 
  1. Показывает подтверждение через confirm
  2. Вызывает `invitesApi.delete(id)`
  3. При успехе показывает toast и редиректит на список
  4. При ошибке показывает toast
- **Связи**: API `invitesApi.delete()`

### formatDate(dateString)
- **Назначение**: Форматирование даты для отображения
- **Логика**: Преобразует ISO строку в формат DD.MM.YYYY HH:MM
- **Используется**: Для отображения дат создания и времени интервью

## URL параметры

### [id]
- **Тип**: Динамический параметр маршрута
- **Назначение**: ID инвайта для просмотра
- **Пример**: `/invites/123` отображает инвайт с ID 123
- **Обработка**: 
  - Читается через `useParams()`
  - Используется для загрузки данных инвайта

## Моковые данные

### mockInvite
Объект инвайта с полями:
- `id`, `candidate_name`, `candidate_email`, `candidate_url`
- `vacancy_title`, `interview_datetime`, `interview_datetime_formatted`
- `status`, `status_display`, `interview_format`
- `google_meet_url`, `google_calendar_url`, `google_drive_file_url`
- `created_at`, `updated_at`

**TODO**: Заменить на реальные данные из `invitesApi.getById()`

## TODO: Замена моковых данных на API

1. ❌ `mockInvite` - заменить на загрузку из `invitesApi.getById()`
2. ❌ `loadInvite()` - реализовать реальный API вызов
3. ❌ `handleRecreateScorecard()` - реализовать реальный API вызов
4. ✅ `handleCopyInvitation()` - использует реальный API
5. ✅ `handleDelete()` - использует реальный API

## Стили

- **Файл**: `invite-detail.module.css`
- **Основной контейнер**: Стили для layout страницы
- **Карточки**: Стили для отображения информации

## Связи с другими страницами

- `/invites/page.tsx` - страница списка инвайтов (кнопка "Назад", редирект после удаления)
- `/invites/[id]/edit/page.tsx` - страница редактирования инвайта (кнопка "Редактировать")
- Внешние ссылки:
  - Google Meet: открывается в новой вкладке
  - Google Calendar: открывается в новой вкладке
  - Google Drive (Scorecard): открывается в новой вкладке
  - Huntflow (кандидат): открывается в новой вкладке

## Особенности

- Детальное отображение всей информации об инвайте
- Управление инвайтом (копирование, пересоздание, редактирование, удаление)
- Ссылки на внешние сервисы (Google Meet, Calendar, Drive, Huntflow)
- Цветовая индикация статуса инвайта
