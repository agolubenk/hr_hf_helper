/**
 * CandidateFieldsSettingsPage (company-settings/candidate-fields/page.tsx) - Страница настроек дополнительных полей кандидатов
 * 
 * Назначение:
 * - Управление дополнительными полями для кандидатов
 * - Настройка кастомных полей, не входящих в стандартный набор
 * - Создание, редактирование и удаление полей
 * 
 * Функциональность:
 * - CandidateFieldsSettings: компонент управления дополнительными полями
 *   - Список дополнительных полей
 *   - Создание нового поля
 *   - Редактирование существующего поля
 *   - Удаление поля
 *   - Настройка типов полей (текст, число, дата, выбор и т.д.)
 *   - Настройка обязательности полей
 * 
 * Связи:
 * - AppLayout: оборачивает страницу в общий layout
 * - CandidateFieldsSettings: компонент с формой управления полями
 * - Sidebar: содержит ссылку на эту страницу в разделе "Настройки рекрутинга"
 * - Huntflow: используется для маппинга полей при интеграции
 * 
 * Поведение:
 * - При загрузке отображает форму управления дополнительными полями
 * - Является частью раздела настроек рекрутинга
 * - Поля используются при создании/редактировании кандидатов
 */

'use client'

import AppLayout from "@/components/AppLayout"
import { Box, Text } from "@radix-ui/themes"
import CandidateFieldsSettings from "@/components/company-settings/CandidateFieldsSettings"
import styles from '../company-settings.module.css'

/**
 * CandidateFieldsSettingsPage - компонент страницы настроек дополнительных полей кандидатов
 * 
 * Функциональность:
 * - Отображает заголовок "Настройки дополнительных полей кандидатов"
 * - Рендерит компонент CandidateFieldsSettings с формой управления полями
 */
export default function CandidateFieldsSettingsPage() {
  return (
    <AppLayout pageTitle="Настройки дополнительных полей кандидатов">
      <Box className={styles.container}>
        {/* Заголовок страницы настроек дополнительных полей */}
        <Text size="6" weight="bold" mb="4" style={{ display: 'block' }}>
          Настройки дополнительных полей кандидатов
        </Text>

        {/* Компонент управления дополнительными полями кандидатов
            - Содержит форму для создания, редактирования и удаления полей
            - Управление типами полей и их обязательностью */}
        <CandidateFieldsSettings />
      </Box>
    </AppLayout>
  )
}
