/**
 * VacancyPromptPage (company-settings/vacancy-prompt/page.tsx) - Страница настройки единого промпта для вакансий
 * 
 * Назначение:
 * - Настройка единого промпта для генерации описаний вакансий через ИИ
 * - Управление шаблоном промпта, используемого при создании вакансий
 * - Настройка параметров для ИИ-генерации описаний
 * 
 * Функциональность:
 * - VacancyPromptSettings: компонент управления промптом для вакансий
 *   - Редактирование текста промпта
 *   - Настройка переменных в промпте
 *   - Предпросмотр результата генерации
 *   - Сохранение промпта как шаблона
 * 
 * Связи:
 * - AppLayout: оборачивает страницу в общий layout
 * - VacancyPromptSettings: компонент с формой управления промптом
 * - Sidebar: содержит ссылку на эту страницу в разделе "Настройки рекрутинга"
 * - AI интеграции: используется при генерации описаний вакансий через Gemini/OpenAI
 * 
 * Поведение:
 * - При загрузке отображает форму редактирования промпта
 * - Является частью раздела настроек рекрутинга
 * - Промпт используется при создании новых вакансий для автоматической генерации описаний
 */

'use client'

import AppLayout from "@/components/AppLayout"
import { Box, Text } from "@radix-ui/themes"
import VacancyPromptSettings from "@/components/company-settings/VacancyPromptSettings"
import styles from '../company-settings.module.css'

/**
 * VacancyPromptPage - компонент страницы настройки единого промпта для вакансий
 * 
 * Функциональность:
 * - Отображает заголовок "Единый промпт для вакансий"
 * - Рендерит компонент VacancyPromptSettings с формой управления промптом
 */
export default function VacancyPromptPage() {
  return (
    <AppLayout pageTitle="Единый промпт для вакансий">
      <Box className={styles.container}>
        {/* Заголовок страницы настройки промпта */}
        <Text size="6" weight="bold" mb="4" style={{ display: 'block' }}>
          Единый промпт для вакансий
        </Text>

        {/* Компонент управления промптом для вакансий
            - Содержит форму для редактирования промпта
            - Настройка переменных и параметров генерации
            - Предпросмотр результата */}
        <VacancyPromptSettings />
      </Box>
    </AppLayout>
  )
}
