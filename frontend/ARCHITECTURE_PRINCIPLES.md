# Архитектурные принципы и важные отметки проекта HR Helper Frontend

## Общая архитектура

### Микросервисная архитектура

**Важно:** Весь проект разбит по микросервисам. Каждый микросервис отвечает за свою область функциональности и может быть разработан, развернут и масштабирован независимо.

**Принципы организации:**
- Каждый микросервис имеет четко определенную область ответственности
- Микросервисы взаимодействуют через четко определенные API
- Изоляция данных и состояния между микросервисами
- Возможность независимого развертывания и масштабирования

**Применение в frontend:**
- Модули приложения организованы по доменам (telegram, aichat, recr-chat и т.д.)
- Каждый модуль может быть выделен в отдельный микросервис при необходимости
- Компоненты и логика изолированы по функциональным областям
- API клиенты организованы по микросервисам

---

## Принципы проектирования

### SOLID принципы

При разработке кода мы строго следуем принципам SOLID для обеспечения поддерживаемости, расширяемости и тестируемости кода.

#### S - Single Responsibility Principle (Принцип единственной ответственности)

**Каждый класс, компонент или модуль должен иметь только одну причину для изменения.**

**Применение:**
- Компоненты React отвечают только за отображение UI
- Хуки отвечают только за управление состоянием или side-эффектами
- Утилиты выполняют только одну конкретную задачу
- API клиенты отвечают только за взаимодействие с конкретным сервисом

**Примеры:**
- `FormattedText` компонент отвечает только за форматирование и отображение текста
- `useToast` хук отвечает только за управление уведомлениями
- `formatFileSize` утилита отвечает только за форматирование размера файла

#### O - Open/Closed Principle (Принцип открытости/закрытости)

**Программные сущности должны быть открыты для расширения, но закрыты для модификации.**

**Применение:**
- Использование композиции вместо наследования
- Расширение функциональности через props и children
- Использование паттернов Strategy, Factory для гибкости
- Абстракции через интерфейсы и типы

**Примеры:**
- Компоненты принимают props для настройки поведения вместо жестко закодированной логики
- API клиенты используют абстракции для возможности замены реализации
- Форматтеры сообщений расширяются через конфигурацию, а не модификацию кода

#### L - Liskov Substitution Principle (Принцип подстановки Барбары Лисков)

**Объекты в программе должны заменяться экземплярами их подтипов без изменения корректности программы.**

**Применение:**
- Интерфейсы и типы определяют контракты, которые должны соблюдаться
- Компоненты, реализующие один интерфейс, взаимозаменяемы
- Наследование используется только когда подтип действительно является подтипом

**Примеры:**
- Все компоненты, реализующие интерфейс `FormattedText`, могут использоваться взаимозаменяемо
- API клиенты следуют единому интерфейсу для возможности замены реализации

#### I - Interface Segregation Principle (Принцип разделения интерфейса)

**Клиенты не должны зависеть от интерфейсов, которые они не используют.**

**Применение:**
- Интерфейсы разделены на мелкие, специфичные контракты
- Компоненты не зависят от props, которые не используют
- API методы разделены по функциональности
- Хуки предоставляют только необходимую функциональность

**Примеры:**
- Разделение интерфейсов для разных типов сообщений (TelegramMessage, AIChatMessage)
- Компоненты получают только необходимые props
- API клиенты разделены по микросервисам, а не объединены в один большой клиент

#### D - Dependency Inversion Principle (Принцип инверсии зависимостей)

**Модули высокого уровня не должны зависеть от модулей низкого уровня. Оба должны зависеть от абстракций.**

**Применение:**
- Зависимость от абстракций (интерфейсов, типов), а не от конкретных реализаций
- Использование dependency injection через props и контексты
- API клиенты инжектируются, а не создаются напрямую
- Тестирование через моки и стабы

**Примеры:**
- Компоненты зависят от интерфейсов данных, а не от конкретных структур
- API клиенты инжектируются через контексты или props
- Утилиты принимают зависимости как параметры

---

### DRY принцип (Don't Repeat Yourself)

**Избегаем дублирования кода, логики и данных. Каждая часть знания должна иметь единственное, однозначное представление в системе.**

**Применение:**

#### 1. Переиспользование компонентов
- Общие UI компоненты вынесены в `@/components`
- Компоненты настраиваются через props, а не копируются
- Композиция компонентов вместо дублирования

**Примеры:**
- `FormattedText` используется в `/aichat` и `/telegram/chats`
- `ToastProvider` используется во всем приложении
- `AppLayout` оборачивает все страницы

#### 2. Переиспользование логики
- Общая логика вынесена в хуки (`useToast`, `useCallback`, `useMemo`)
- Утилиты для общих операций (форматирование, валидация)
- Константы и конфигурация в отдельных файлах

**Примеры:**
- `formatFileSize` используется в разных местах
- Логика форматирования текста централизована в `FormattedText`
- Константы API endpoints в конфигурационных файлах

#### 3. Переиспользование типов и интерфейсов
- Общие типы вынесены в отдельные файлы
- Интерфейсы определены один раз и переиспользуются
- Типы экспортируются для использования в других модулях

**Примеры:**
- Интерфейсы сообщений определены в соответствующих модулях
- Общие типы в `@/types` или в модулях

#### 4. Избегание дублирования стилей
- Общие стили в CSS модулях
- Переиспользование классов стилей
- CSS переменные для общих значений

**Примеры:**
- `telegram.module.css` содержит общие стили для всех страниц Telegram
- Использование CSS переменных Radix UI для темизации

#### 5. Единая точка истины для данных
- Состояние управляется централизованно (через контексты или state management)
- API клиенты единообразны
- Конфигурация в одном месте

**Примеры:**
- `ToastContext` - единая точка управления уведомлениями
- API клиенты следуют единому паттерну

---

## Масштабируемость данных

### Принципы проектирования для масштабирования

**Важно:** Архитектура спроектирована с учетом возможности масштабирования данных в будущем. Все решения принимаются с учетом потенциального роста объема данных и нагрузки.

#### 1. Пагинация и виртуализация

**Применение:**
- Списки данных реализуются с поддержкой пагинации
- Большие списки используют виртуализацию для производительности
- Загрузка данных по частям (lazy loading)

**Текущее состояние:**
- ✅ Списки чатов поддерживают фильтрацию (готовы к пагинации)
- ⚠️ TODO: Реализовать виртуализацию для больших списков чатов
- ⚠️ TODO: Добавить пагинацию сообщений в чатах
- ⚠️ TODO: Реализовать infinite scroll для истории сообщений

**Примеры:**
- Список чатов в `/telegram/chats` готов к добавлению пагинации
- История сообщений может быть расширена пагинацией

#### 2. Кэширование данных

**Применение:**
- Кэширование ответов API для уменьшения нагрузки
- Локальное кэширование часто используемых данных
- Инвалидация кэша при обновлении данных

**Текущее состояние:**
- ⚠️ TODO: Реализовать кэширование ответов API
- ⚠️ TODO: Добавить локальное хранилище для часто используемых данных
- ⚠️ TODO: Реализовать стратегию инвалидации кэша

**Рекомендации:**
- Использовать React Query или SWR для кэширования API запросов
- LocalStorage/SessionStorage для пользовательских настроек
- Service Workers для офлайн-кэширования

#### 3. Оптимистичные обновления

**Применение:**
- UI обновляется немедленно при действиях пользователя
- Синхронизация с сервером происходит в фоне
- Откат изменений при ошибке

**Текущее состояние:**
- ✅ Частично реализовано в отправке сообщений (UI обновляется сразу)
- ⚠️ TODO: Добавить откат изменений при ошибке
- ⚠️ TODO: Реализовать очередь синхронизации для офлайн-режима

#### 4. Ленивая загрузка (Lazy Loading)

**Применение:**
- Компоненты загружаются по требованию
- Данные загружаются только когда нужны
- Изображения загружаются с lazy loading

**Текущее состояние:**
- ✅ Next.js автоматически поддерживает code splitting
- ✅ Изображения могут использовать lazy loading
- ⚠️ TODO: Реализовать динамический импорт для больших компонентов

#### 5. Дебаунсинг и троттлинг

**Применение:**
- Поиск использует debounce для уменьшения количества запросов
- Скролл использует throttle для оптимизации обработки событий
- API запросы группируются при возможности

**Текущее состояние:**
- ✅ Поиск в чатах работает в реальном времени (можно добавить debounce)
- ⚠️ TODO: Добавить debounce для поиска (300-500ms)
- ⚠️ TODO: Добавить throttle для обработки скролла

#### 6. Индексация и поиск

**Применение:**
- Данные структурированы для эффективного поиска
- Использование индексов для быстрого доступа
- Оптимизация запросов поиска

**Текущее состояние:**
- ✅ Поиск по чатам работает на клиенте (готов к переносу на сервер)
- ⚠️ TODO: Реализовать полнотекстовый поиск на сервере
- ⚠️ TODO: Добавить индексацию сообщений для быстрого поиска

#### 7. Архитектура данных

**Применение:**
- Нормализация данных для избежания дублирования
- Денормализация где необходимо для производительности
- Эффективные структуры данных

**Текущее состояние:**
- ✅ Данные чатов нормализованы (каждый чат имеет уникальный ID)
- ✅ Сообщения связаны с чатами через ID
- ⚠️ TODO: Оптимизировать структуру данных для больших объемов

#### 8. Мониторинг и метрики

**Применение:**
- Отслеживание производительности запросов
- Мониторинг использования памяти
- Метрики для выявления узких мест

**Текущее состояние:**
- ⚠️ TODO: Добавить мониторинг производительности
- ⚠️ TODO: Реализовать логирование медленных запросов
- ⚠️ TODO: Добавить метрики использования памяти

---

## Рекомендации по разработке

### При добавлении нового функционала

1. **Проверка на дублирование:**
   - Есть ли уже похожий компонент/логика?
   - Можно ли переиспользовать существующий код?
   - Нужно ли создать общий компонент/утилиту?

2. **Следование SOLID:**
   - Компонент/функция имеет одну ответственность?
   - Можно ли расширить без модификации?
   - Зависимости инжектируются, а не создаются напрямую?

3. **Учет масштабируемости:**
   - Как поведет себя при росте данных?
   - Нужна ли пагинация/виртуализация?
   - Как оптимизировать производительность?

4. **Микросервисная архитектура:**
   - К какому микросервису относится функционал?
   - Как изолировать данные и логику?
   - Можно ли развернуть независимо?

### При рефакторинге

1. **Выявление дублирования:**
   - Поиск повторяющихся паттернов
   - Выделение общей логики
   - Создание переиспользуемых компонентов

2. **Улучшение архитектуры:**
   - Применение SOLID принципов
   - Улучшение разделения ответственности
   - Оптимизация зависимостей

3. **Оптимизация производительности:**
   - Добавление кэширования
   - Реализация пагинации/виртуализации
   - Оптимизация рендеринга

---

## Чеклист для code review

### SOLID принципы
- [ ] Компонент/класс имеет одну ответственность
- [ ] Функциональность расширяется без модификации
- [ ] Интерфейсы разделены по функциональности
- [ ] Зависимости инжектируются, а не создаются напрямую

### DRY принцип
- [ ] Нет дублирования кода
- [ ] Общая логика вынесена в утилиты/хуки
- [ ] Компоненты переиспользуются
- [ ] Стили не дублируются

### Масштабируемость
- [ ] Учтена возможность роста данных
- [ ] Реализована пагинация для больших списков (если необходимо)
- [ ] Оптимизированы запросы к API
- [ ] Добавлено кэширование где необходимо

### Микросервисная архитектура
- [ ] Функционал изолирован по доменам
- [ ] API клиенты организованы по микросервисам
- [ ] Нет жестких зависимостей между модулями

---

## Дополнительные ресурсы

### SOLID принципы
- [SOLID Principles in TypeScript](https://khalilstemmler.com/articles/solid-principles/solid-typescript/)
- [SOLID Principles in React](https://dmitripavlutin.com/solid-principles-react/)

### DRY принцип
- [DRY Principle](https://en.wikipedia.org/wiki/Don%27t_repeat_yourself)
- [Code Reusability in React](https://react.dev/learn/reusing-logic-with-custom-hooks)

### Масштабируемость
- [React Performance Optimization](https://react.dev/learn/render-and-commit)
- [Web Performance Best Practices](https://web.dev/performance/)

---

**Версия документа:** 1.0.0  
**Последнее обновление:** 2026-01-27  
**Автор:** HR Helper Development Team

**Важно:** Этот документ должен регулярно обновляться при изменении архитектурных решений и добавлении новых принципов.
